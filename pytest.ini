[pytest]
norecursedirs = test/fixtures/ test/helpers/ scratch/*
addopts = -m "not cli and not bito"
markers =
    bito: marks tests that require the optional bito package (run with '-m bito')
    cli: marks tests that exercise the CLI end-to-end (run with '-m cli')